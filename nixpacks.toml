[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev", "cd backend && npm ci --omit=dev"]

[phases.build]
cmds = ["echo 'Backend only - no build needed'"]

[start]
cmd = "node backend/server.js"
